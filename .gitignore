local.nix

# Secrets — never commit the real age private key.
# The real key lives ONLY at ~/.config/sops/age/keys.txt (outside this repo).
.config/sops/age/keys.txt

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
