local.nix

# nix build output symlink (darwin-rebuild build)
result

# Secrets — never commit the real age private key.
# The real key lives ONLY at ~/.config/sops/age/keys.txt (outside this repo).
.config/sops/age/keys.txt

# Devenv
.devenv*
devenv.local.nix
# devenv.lock re-locks per machine (different devenv versions) and isn't the system
# flake.lock — untracked to stop cross-machine churn blocking `git pull`.
devenv.lock

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
